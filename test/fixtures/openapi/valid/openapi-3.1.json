{
  "openapi": "3.1.0",
  "info": { "title": "OpenAPI 3.1 JSON", "version": "1.0.0" },
  "paths": {
    "/search": {
      "get": {
        "parameters": [
          {
            "name": "kind",
            "in": "query",
            "required": false,
            "schema": { "type": ["string", "null"], "enum": ["user", "team", null] }
          }
        ],
        "responses": { "200": { "description": "OK" } }
      }
    }
  }
}
