openapi: 3.1.0
info: { title: OpenAPI 3.1 YAML, version: 1.0.0 }
paths:
  /messages:
    post:
      requestBody:
        content:
          text/plain:
            schema: { type: string, maxLength: 4096 }
      responses:
        '202': { description: Accepted }
