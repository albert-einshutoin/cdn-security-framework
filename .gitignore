# Dependencies
node_modules/

# Environment / secrets
.env
.env.local
*.pem

# OS / editor
.DS_Store
*.swp
*.swo
Thumbs.db

# AI / internal docs (not for distribution)
CLAUDE.md
docs/SELF-REVIEW.md

# Build / logs
# dist/ は npm run build で生成する。リポジトリには含めず、ユーザー環境でビルドさせる。CI で検証する場合は CI 内で build して diff チェックする。
dist/
/examples/*/dist/
/examples/*/policy/
/examples/*/node_modules/
*.log
.wrangler/

# c8 coverage output
coverage/
.nyc_output/

# doctor subcommand report — regenerated on every run, captured as CI artifact
doctor-report.json

# Local dev override — users copy a profile here; never check it in
policy/security.yml
